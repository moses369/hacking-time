import { SortableHeadDirective } from './sortable-head.directive';

describe('SortableHeadDirective', () => {
  it('should create an instance', () => {
    const directive = new SortableHeadDirective();
    expect(directive).toBeTruthy();
  });
});
